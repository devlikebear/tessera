# AGENTS.md

이 문서는 Tessera repo에서 Codex와 함께 작업할 때 항상 따를 개발 플로우를 정의한다.

## 기본 원칙

- 커밋 메시지는 `feat:`, `fix:`, `chore:` 형식을 사용한다.
- 테스트 우선으로 작업한다. 기능 변경이나 버그 수정은 먼저 실패 테스트를 작성하고, 그 테스트를 통과시키는 최소 구현으로 진행한다.
- 문서, 릴리스 메타데이터, 태그 생성처럼 실행 코드가 바뀌지 않는 작업은 테스트가 불필요할 수 있다. 이 경우 최종 보고에 테스트를 생략한 이유를 명시한다.
- 사용자가 명시하지 않은 기존 변경은 되돌리지 않는다.

## 작업 시작 절차

1. 변경 전에 `git status --short --branch`를 확인한다.
2. 현재 브랜치, 원격과의 동기화 상태, 이미 존재하는 변경 파일을 요약한다.
3. 요청과 관련된 파일만 읽고, 변경 범위를 먼저 짧게 설명한다.
4. 기존 사용자 변경이 있으면 작업 범위를 분리해서 다루고, 충돌 가능성이 있을 때만 확인 질문을 한다.

## 개발 플로우

1. 요구사항을 작은 작업 단위로 나눈다.
2. 동작 변경이 있으면 실패 테스트를 먼저 추가한다.
3. 기존 코드 스타일과 패턴을 따라 구현한다.
4. 변경 범위에 맞는 테스트를 실행한다. Go 코드는 기본적으로 `go test ./...`를 사용한다.
5. `git diff --check`로 공백/패치 오류를 확인한다.
6. 최종 응답에는 변경 내용, 검증 결과, 테스트 미실행 사유가 있으면 그 사유를 포함한다.

## 릴리스와 태그

- `VERSION.txt`를 릴리스 버전의 기준으로 삼는다.
- `CHANGELOG.md`에는 같은 버전의 릴리스 노트를 유지한다.
- 태그 이름은 `v<VERSION.txt 값>` 형식을 사용한다. 예: `VERSION.txt`가 `0.1.0`이면 `v0.1.0`.
- 태그 생성 전에는 작업트리가 깨끗한지 확인한다.
- 태그는 annotated tag로 만든다.

```sh
git tag -a vX.Y.Z -m "Release vX.Y.Z"
git push origin vX.Y.Z
```

- 태그 push 후 `git ls-remote --tags origin "vX.Y.Z" "vX.Y.Z^{}"`로 원격 태그를 확인한다.

## PR 규칙

- PR을 만들 때는 repo의 PR 템플릿이 있으면 그 형식을 엄수한다.
- PR 템플릿이 없으면 최소한 변경 요약, 테스트 결과, 릴리스/태그 영향 여부를 포함한다.
- PR 설명에는 실제로 실행한 검증 명령만 적는다.
